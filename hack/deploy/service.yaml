apiVersion: v1
kind: Service
metadata:
  name: projectc-wallet
  labels:
    app: projectc-wallet
spec:
  selector:
    app: projectc-wallet
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
